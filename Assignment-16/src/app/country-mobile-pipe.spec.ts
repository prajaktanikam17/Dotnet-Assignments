import { CountryMobilePipe } from './country-mobile-pipe';

describe('CountryMobilePipe', () => {
  it('create an instance', () => {
    const pipe = new CountryMobilePipe();
    expect(pipe).toBeTruthy();
  });
});
